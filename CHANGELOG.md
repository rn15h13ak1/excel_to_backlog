# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added
- TLS 証明書の検証が既定で有効であることをテストで固定した。挙動は正しかったが
  テストが無く、既定を false 側へ倒す変更は CI を素通りしていた（検証を外す枝が
  実行されるぶんカバレッジはむしろ上がり、下限にも引っかからない）
- `--debug` 出力のテスト。とくに GET はクエリから `apiKey` を除いてから出して
  いるが、その除去に検証が無かった。落ちると API キーが stderr に漏れる
  （`backlog_client.py` のカバレッジ 85% → 99%）
- `CLAUDE.md`。共通規約（`../ws-conventions/README.md`）に対する本リポジトリ固有の
  事情を書く。作業のたびに実行する検査、元 Excel を変更しない方針、設定ファイルの
  扱いをまとめた

### Fixed
- 変異テスト（週次）の成果物が毎回空だった。パスが mutmut 2.x の
  `.mutmut-cache` のままで、使っている 3.7 は `mutants/` に出力する。
  `if-no-files-found: ignore` と重なり、「成功」と表示しながら何も
  保存していなかった。見つからなければ失敗させるようにした

### Changed
- CI の対象に Python 3.13 / 3.14 を加えた。`requires-python = ">=3.10"` と
  宣言しているのに 3.10 と 3.12 しか見ておらず、新しい Python で依存の挙動が
  変わっても気づけなかった
- **修正のたびに commit / push を自動で行うようにした。** 共通規約 A は「利用者が
  明示的に指示したときだけ」としているが、本リポジトリは利用者 1 人の開発環境に
  あり、区切りごとに確認を挟んでも判断材料が増えず往復だけが増えるため。
  全テストが通ることを commit の前提とする。逸脱の理由は `CLAUDE.md` に残した
- `.gitignore` に資格情報の定型（`.env` / `.env.*` / `secrets.yaml` / `credentials*` /
  `*.pem` / `*.key` / `*.local.yaml`）を追加した。これまで `config.yaml` を名指しで
  除外しているだけで、他の置き場は素通りしていた。一度コミットすると Git 履歴から
  消すのが難しいため、先に塞ぐ
- 文書中の Windows パスの例を、ホームディレクトリ配下のものから `C:\work\…` に
  変えた。バックスラッシュが YAML のエスケープ文字になることを示すのが目的で、
  ホームディレクトリである必要はない（共通規約の個人情報検査に引っかかっていた）

## [0.7.0] - 2026-08-31

実際の Excel で動かして分かったことを反映しました。

### Added
- 1 件の内容が複数行に分かれている表を 1 件にまとめる（`merge_continuation_rows`）。
  セル内に収まらない内容を次の行へ書き足す運用への対応。継続行の判定は
  `required_cols` の列がすべて「空」または「直前の行と同じ値」であること。
  書式で文字色を変えて見えなくしている場合もあるため、空欄だけを条件にしない
- 継続行の連結は空行区切り（Markdown の段落として分かれる）。値が空の列には
  区切りを入れず、すでに同じ値がある列は重複させない。1 件につき 1 つしか値を
  持てない列（件名・日付・担当者・ステータス・カスタム属性）は警告して無視する
- どの行がどこへ結合されたかを実行時に表示する。結合されなかった場合は判定条件を
  示す（`required_cols` の指定が実態と合っていないことに気づけるように）
- 更新前に送信内容と既存課題を比較し、変更なしの行は送信も確認もしない。
  以前は更新を実行して Backlog のエラーを見るまで「変更なし」と分からなかった
- 出力先フォルダの指定（`--output-dir` と設定の `output_dir`）

### Changed
- 同名ヘッダーに連番（`(2)`）を付けず、Excel の表記をそのまま使う。Excel と
  見比べたときに名前が違うと混乱するため。列名での参照は左端の列
- 同名ヘッダーの列を本文にすべて出力する。左端だけを使うと右側の列が黙って消える
- 実行時の確認を 1 件ずつに戻した。まとめて更新すると、件数が多いときに何が
  起きたのか人間の側で把握しきれない
- 確認するのは新規作成のみとし、更新は確認せず実行する。課題が増えるのは新規作成の
  ときだけであり、更新まで都度確認すると件数が多くなって判断が鈍る
- 実行前の件数の事前算出をやめた。算出には全行を Backlog と照合する必要があり、
  行数に比例して「何も起きない待ち時間」が伸びていた。件数はドライランで確認する
- プレビュー（`preview_*.md`）と実行ログ（`run_*.csv`）を `output/` フォルダに
  出力する。設定ファイルと同じ場所に出すと、実行のたびに増えて元のファイルに混ざる

### Fixed
- Backlog の「No comment content.（code=7）」を変更なしとして扱う。更新時に
  エラーとして報告されていた
- `rich_text` 有効時、書式付きの行が結合されず本文が先頭行だけになっていた。
  ログには「継続行を結合」と出るのに Markdown には反映されない状態だった

## [0.6.0] - 2026-08-29

設定を書くときに使うオプションを足し、設定ミスを実行前に見つけるようにしました。

### Added
- `--list-master` で設定に使える名前（種別・優先度・ステータス・担当者・
  カスタム属性）を一覧表示する
- `--show-columns` で Excel から読み取れる列名を一覧表示する（Backlog へは接続
  しないため、設定を書いている途中で使える）
- `--limit N` で各ソースの先頭 N 行だけ処理する（初回に少数だけ試す用）
- 設定キーの綴り間違いと列挙値の誤りを検出する。キー名が違っても YAML としては
  正しいため、これまでは黙って無視されていた
- 最小構成の設定サンプル（`config.minimal.yaml`）
- `excel-to-backlog` コマンドとして実行できるようにした。python のパスや作業
  ディレクトリを意識せずに済む
- 実行前の確認に作成予定・更新予定の件数を表示する（0.7.0 で削除）

### Changed
- 分岐カバレッジで測り、95% を下限にした。`try` の範囲を変えるような構造的な
  変更は行数を変えないため、行カバレッジでは検出できない経路が残る
- 変異テスト（mutmut）を導入した。カバレッジが「テストが触れていない場所」を
  示すのに対し、変異テストは「触れているが検証できていない場所」を示す

### Fixed
- YAML でキーだけ書いて中身が空の設定を検出する
- 実行時に読まれない設定項目を列名検証の対象から外す（使っていない設定の列名で
  停止していた）

## [0.5.0] - 2026-08-29

中断・失敗しても復旧できるようにし、振る舞いをテストで固定しました。

### Added
- リトライ（429 は必ず、通信エラー・5xx は冪等な要求のみ）。POST は再送しない
- 実行ログ `run_*.csv` と、`--resume` で作成・更新済みの行を飛ばして再開する機能
- Strict Open XML の判定。openpyxl は Transitional 形式にしか対応しておらず、
  読めない理由が `list index out of range` としか出なかった。名前空間を見て
  変換方法を案内して停止する
- 設定が参照する列名を実行前に検証して停止する。列名が 1 つでも一致しないと、
  フィルター条件が無視されて全行が対象になる等の無言の誤動作が起きる
- 表示・ログに Excel シート上の行番号を使う（1 始まりの連番では Excel を辿れない）
- フィールドが落ちた警告を行に紐づけ、実行ログに記録する
- pytest の導入と GitHub Actions での自動実行。元 Excel を変更しないこと・
  upsert の振る舞い・CLI・HTTP 層・プレビュー生成・`filter_groups` を固定した

### Changed
- 件名照合を行ごとの検索から一括取得＋索引に置き換えた（N+1 の解消）。
  `keyword` 検索は本文にもマッチするため、500 行で千回規模の往復になっていた
- ライブラリ内の `sys.exit` を例外にした。失敗の握り潰しと誤報告の解消
- 設定ミスを行スキップではなくソース中止として扱い、終了コードに反映する。
  設定のタイプミスが「全行スキップ」というデータ不備のような報告になっていた
- `RowPlan` を導入し、ドライランが実行を予測できるようにした
- 読み込みパイプラインを `load_source` に共通化し、`id()` 依存を解消した。
  フィルターがコピーを返した瞬間に本文が静かに元テキストへ戻る作りだった

### Fixed
- 新規作成時に完了ステータスを直接指定するとエラーになる。未対応で作成してから
  ステータスを変更する 2 段階にした
- 重複ヘッダーによるデータ消失と、日付の無言の欠落
- 中断してもそれまでの集計がサマリーに残るようにした。通信エラーがトレースバックの
  まま抜けて、何件作成済みかを知る手段がターミナルのログしか無かった
- ドライランで確認プロンプトを出さない。`stdin` が無い環境も検出する（非対話環境で
  毎回 `EOFError` になり、1 件も作らずに「作成: 0 件」と正常終了していた）
- 選択肢型カスタム属性の選択肢一覧が空のとき、名前を ID として送っていた
- 非選択肢型カスタム属性で `value_separator` の分割結果を黙って捨てていた
- 同じ件名の行が複数あるとき、ドライランの予測が実行結果とずれていた
- 照合の失敗で `UnboundLocalError` になる問題と、スキップ行での不要な待機
- 変更なし・ステータス変更失敗の行で警告が失われていた
- 再開の行識別に行番号を含めた。再開スキップも実行ログに残す
- Excel 読み込みエラー時にトレースバックを出力して原因特定を容易にした

## [0.4.0] - 2026-04-09

Excel の取り消し線を Markdown に反映できるようにしました。

### Added
- Excel の取り消し線を Markdown の `~~text~~` に変換する `rich_text` オプション。
  `description_format: auto` で追加し、`template` の `{{auto}}` にも対応した
  （`{{列名}}` はプレーンテキストのまま）
- README.md（使い方・機能別リファレンス・トラブルシューティング）

### Fixed
- 日付セルの取り消し線が反映されなかった。`cell.font.strike` を `CellRichText`
  チェックより先に評価するようにした
- `CellRichText` のセルで取り消し線が検出できなかった。セルレベルの取り消し線を
  文字列ランへ継承する形に落ち着いた（Excel が「セル全体に取り消し線 → 一部を
  解除」した際の保存形式に対応するため）
- 複数行セルで 1 行目にしか取り消し線が反映されなかった

## [0.3.0] - 2026-04-04

カスタム属性の値の作り分けに対応しました。

### Added
- 担当者のデフォルト値（`default_assignee`）
- カスタム属性の値の変換テーブル（`value_map`）。キーには正規表現も書ける
- カスタム属性の複数選択対応（`value_separator`）
- `--debug` に Backlog レスポンスの `customFields` を出力する（反映されたかの確認用）

### Fixed
- POST/PATCH ボディの `[]` をパーセントエンコードしていたため、
  `customField_{id}[]` が届いていなかった
- typeId 5/8（単一リスト・ラジオ）を `[]` なしの単一値形式で送るようにした
- `value_map` の正規表現マッチに `re.DOTALL` を追加した（セル値の改行への対応）

## [0.2.0] - 2026-04-02

書き込み前の確認と、取りこぼしの防止を入れました。

### Added
- execute モードで新規作成前にユーザー確認を行う
- `required_cols` で必須列が空の行をスキップする
- Backlog ステータスの Excel 連携（`status_col`）
- `summary_template` の改行・タブを正規化し、`match_summary` の比較にも同じ
  正規化を使う（同じ件名が別物として扱われないように）

### Changed
- `except` 節の冗長な例外型を統一し、関数内 `import` をやめた
- `config.sample.yaml` に不足していたコメントを追加した

### Fixed
- 件名検索がページネーションしておらず、101 件目以降の既存課題を見つけられずに
  新規作成していた
- `BacklogNoChangeError` 発生時に Backlog の実メッセージをログ出力するようにした
- `build_master_labels` の担当者の逆引きを表示名に固定した
- 件名の正規化で、改行・タブをスペース置換から除去に変更した

## [0.1.0] - 2026-03-26

最初の実装。

### Added
- Excel を読んで Backlog の課題を作成・更新する本体
- `config.yaml` による複数ソース定義（`sources`）。1 つの設定で複数の Excel を扱う
- 本文の組み立て方を 2 種類用意（`description_format`）
  - `auto`: 列ごとに見出しを付けた Markdown（excel_md_tool と同じ形式）
  - `template`: 自由記述。`{{auto}}` で auto の出力を埋め込める
- `summary_template` で複数列から件名を組み立てる
- `{{_source_name}}` / `{{_excel_path}}` / `{{_excel_sheet}}` のメタキー
- `due_date_col` / `start_date_col` の `{{列名}}` テンプレート展開
- フィルター条件の OR 指定（`values`）と、複合キーによる OR（`filter_groups`）
- テンプレートの条件ブロック `{{#列名}}...{{/列名}}`（値が空ならブロックごと非表示）
- `--preview` で登録予定の内容を Markdown ファイルに出力する
  （`sources[].name` 単位で分割）

### Fixed
- Windows パスのバックスラッシュによる YAML パースエラー
- 優先度・種別の名前不一致を黙って無視していた（エラーとして検出するようにした）
- 種別・優先度・担当者の ID 逆引きが混在していた（カテゴリ別に分離）
- ヘッダーセル内の改行文字を除去する
- 担当者を表示名・ログイン ID のどちらでも解決できるようにし、一致しない場合は
  利用可能なユーザー一覧を表示する
- 同一内容の更新エラー（HTTP 400 / code=7）をスキップ扱いに変更した
